export function addOrUpdateUser(sub: string, name: string, given_name: string, 
  family_name: string, picture: string, email: string) {
  console.log(sub, name, given_name, family_name, picture, email);
  return {}
}